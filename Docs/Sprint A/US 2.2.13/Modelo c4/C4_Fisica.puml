@startuml C4_Manage_Qualifications_Physical_View
title C4 – Manage Qualifications (Physical / Deployment View)

node "Kubernetes Cluster / Virtual Machine" as Cluster {
  folder "PLMS Namespace" {
    node "Qualifications API (Docker Container)" as API
    node "Frontend (Static SPA - React/Angular)" as Frontend
  }
}

node "RDS / SQL Server Instance" as RDS {
  database "PLMS Database" as Database
}

Frontend --> API : REST / HTTPS
API --> Database : EF Core / SQL

@enduml