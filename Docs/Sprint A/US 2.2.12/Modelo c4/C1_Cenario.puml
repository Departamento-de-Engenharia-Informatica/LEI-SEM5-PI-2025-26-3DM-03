@startuml C1_Manage_Resources_Scenario_View
title C1 – Manage Resources (Use Case Scenario)

actor "Port Authority Officer" as Officer
rectangle "PLMS (Port Logistics Management System)" as PLMS

Officer --> PLMS : Requests to list/manage Resources via UI
PLMS --> Officer : Shows list / edit form

PLMS --> "External IAM Provider (OAuth 2.0)" : Delegates authentication
PLMS --> "PLMS Database" : Persists Resources

note right of PLMS
 Typical scenario: the Officer opens the UI, authenticates and creates/edits
 resources (cranes, trucks, etc.) which are persisted in the database.
end note

@enduml