@startuml C2_Manage_Resources_Process_View
title C2 – Manage Resources (Container / Process View)

actor "Port Authority Officer" as Officer
participant "Frontend Web App\n(React / Angular)" as Frontend
participant "Resources Management API\n(ASP.NET Core)" as API
database "PLMS Database\n(SQL / InMemory)" as DB

Officer -> Frontend : Opens Resources UI
Frontend -> API : GET/POST/PUT /api/Resources
API -> DB : CRUD Operations (EF Core)
API -> "External IAM Provider" : Validates Token

@enduml