@startuml C3_Manage_Resources_Physical_View
title C3 – Manage Resources (Physical / Component Deployment)

node "App Server (Docker Host)" as App {
  component "Resources API (ASP.NET Core)" as API
  component "ResourceMapper (Library)" as Mapper
}

node "DB Server (SQL Server)" as DB {
  database "PLMS Database" as Database
}

API --> Mapper : uses
API --> Database : connects via EF Core

@enduml