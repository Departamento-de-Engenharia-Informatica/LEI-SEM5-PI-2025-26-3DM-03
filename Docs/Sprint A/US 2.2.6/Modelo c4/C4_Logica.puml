@startuml C4_Manage_Representatives_Logical_View
title C4 — Componentes | Vista Lógica — Manage Representatives (PLMS)
left to right direction

skinparam node {
  BackgroundColor White
  BorderColor Black
  FontColor Black
}
skinparam database {
  BackgroundColor Wheat
  BorderColor Black
  FontColor Black
}
skinparam artifact {
  BackgroundColor HoneyDew
  BorderColor Black
  FontColor Black
}

actor "Port Authority Officer" as Officer

node "User Device\n(Laptop / Browser)" as Client {
  artifact "Frontend Web App\n(React / Angular)" as Frontend
}

node "Application Server\n(Docker Container / IIS)" as AppServer {
  artifact "Representatives Management API\n(ASP.NET Core Web API)" as API
}

node "Database Server\n(SQL / EF Core)" as DBServer {
  database "PLMS Database" as Database
}

Officer --> Frontend : Manage representatives\n(Create / Update / Deactivate) (HTTPS)
Frontend --> API : REST API calls (HTTPS / JSON)
API --> Database : CRUD Representatives (EF Core)

@enduml
